<% if model.errors.any? %>
	<div>

			<h2 style="color:red"><%= pluralize(model.errors.count,"error") %> prohibited this obj from being saved:</h2>

		<ul>
			<% model.errors.full_messages.each do |msg| %>
				<li style="color:red"><%= msg %></li>
			<% end %>
		</ul>

	</div>
<% end %>
